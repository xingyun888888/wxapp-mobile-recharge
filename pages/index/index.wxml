<view class="map-wrap">
  <view class="search-wrap">
    <navigator url="/pages/usercenter/usercenter" class="user-info">
      <image src="{{logo}}"></image>
    </navigator>
    <view class="input-like" bindtap="chooseLocation">
      <i class="iconfont icon-sousuo_sousuo"></i>
      搜索位置查找附近充电宝
    </view>
    <view class="to-list" bindtap="navigateTo">
      <i class="iconfont icon-liebiao"></i>
    </view>
  </view>

  <view class="no-recharge" wx:if="{{amount<80}}">
    <view class="tips">您尚未缴纳押金</view>
    <view class="recharge">充押金</view>
    <i class="iconfont icon-more"></i>
  </view>

  <map 
    id="map" 
    longitude="{{lng}}" 
    latitude="{{lat}}" 
    scale="{{scale}}" 
    class="map"
    markers="{{markers}}"
    controls="{{controls}}"
    style="height: {{mapHeight}}px"
    show-location
    bindcontroltap="clickControl"
    bindmarkertap="clickMarker"
    bindregionchange="regionchange"
    bindtap="clickMap"
  ></map>
</view>